@model RakietaLogikaBiznesowa.Models.UserAndBank


@{
    ViewBag.Title = "AddBankAccount";
}


<h2>Dodaj konto bankowe do klienta</h2>

@using (@Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Wybierz konto i użytkownika</h4>
        <hr/>
        @Html.ValidationSummary(true,"",new {@class = "text-danger"})
        <div class="form-group">
            @Html.LabelFor(e => e.User, "User Login", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("User", null, htmlAttributes: new {@class = "form-control"})
                @Html.ValidationMessageFor(e => e.User)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(e => e.Bank, "Bank Id", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("Bank", null, htmlAttributes: new {@class = "form-control"})
                @Html.ValidationMessageFor(e => e.Bank)
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Add" class="btn btn-default" />
            </div>
        </div>
    </div>
        

}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
